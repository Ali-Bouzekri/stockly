<form method="post" action="{{ path('app_ligne_cmd_extern_delete', {'numL_CE': ligne_cmd_extern.numL_CE}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne_cmd_extern.numL_CE) }}">
    <button class="btn">Delete</button>
</form>
