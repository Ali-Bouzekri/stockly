<form method="post" action="{{ path('app_ligne_liv_intern_delete', {'numL_LI': ligne_liv_intern.numL_LI}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne_liv_intern.numL_LI) }}">
    <button class="btn">Delete</button>
</form>
