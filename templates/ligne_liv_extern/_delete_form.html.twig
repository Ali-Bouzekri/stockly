<form method="post" action="{{ path('app_ligne_liv_extern_delete', {'numL_LE': ligne_liv_extern.numL_LE}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne_liv_extern.numL_LE) }}">
    <button class="btn">Delete</button>
</form>
