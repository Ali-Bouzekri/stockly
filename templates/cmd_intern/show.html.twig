{% extends 'base.html.twig' %}

{% block title %}CmdIntern{% endblock %}

{% block body %}
    <h1>CmdIntern</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCmdInt</th>
                <td>{{ cmd_intern.idCmdInt }}</td>
            </tr>
            <tr>
                <th>DateCI</th>
                <td>{{ cmd_intern.dateCI ? cmd_intern.dateCI|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ cmd_intern.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cmd_intern_index') }}">back to list</a>

    <a href="{{ path('app_cmd_intern_edit', {'idCmdInt': cmd_intern.idCmdInt}) }}">edit</a>

    {{ include('cmd_intern/_delete_form.html.twig') }}
{% endblock %}
