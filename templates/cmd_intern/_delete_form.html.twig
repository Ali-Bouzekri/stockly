<form method="post" action="{{ path('app_cmd_intern_delete', {'idCmdInt': cmd_intern.idCmdInt}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cmd_intern.idCmdInt) }}">
    <button class="btn">Delete</button>
</form>
