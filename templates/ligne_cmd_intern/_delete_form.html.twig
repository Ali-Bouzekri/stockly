<form method="post" action="{{ path('app_ligne_cmd_intern_delete', {'numL_CI': ligne_cmd_intern.numL_CI}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne_cmd_intern.numL_CI) }}">
    <button class="btn">Delete</button>
</form>
