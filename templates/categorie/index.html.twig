{% extends 'base.html.twig' %}

{% block title %}Categorie index{% endblock %}

{% block body %}
    <h1>Categorie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdCategory</th>
                <th>NomCategory</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie in categories %}
            <tr>
                <td>{{ categorie.idCategory }}</td>
                <td>{{ categorie.nomCategory }}</td>
                <td>
                    <a href="{{ path('app_categorie_show', {'idCategory': categorie.idCategory}) }}">show</a>
                    <a href="{{ path('app_categorie_edit', {'idCategory': categorie.idCategory}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_new') }}">Create new</a>
{% endblock %}
