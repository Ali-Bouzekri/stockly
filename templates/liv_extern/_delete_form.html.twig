<form method="post" action="{{ path('app_liv_extern_delete', {'idLivExt': liv_extern.idLivExt}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liv_extern.idLivExt) }}">
    <button class="btn">Delete</button>
</form>
