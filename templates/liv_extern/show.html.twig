{% extends 'base.html.twig' %}

{% block title %}LivExtern{% endblock %}

{% block body %}
    <h1>LivExtern</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdLivExt</th>
                <td>{{ liv_extern.idLivExt }}</td>
            </tr>
            <tr>
                <th>DateLE</th>
                <td>{{ liv_extern.dateLE ? liv_extern.dateLE|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_liv_extern_index') }}">back to list</a>

    <a href="{{ path('app_liv_extern_edit', {'idLivExt': liv_extern.idLivExt}) }}">edit</a>

    {{ include('liv_extern/_delete_form.html.twig') }}
{% endblock %}
